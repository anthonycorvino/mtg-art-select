import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Call the React renderer to start an instance of the App hook
ReactDOM.render(<App />, document.getElementById('root'));

